import './Hero.css'
const hero = ({heroData, heroCount, setHeroCount, setPlayStatus, playStatus}) => {
    return (
        <div className='hero'>
            
        </div>
    )       
}

export default hero